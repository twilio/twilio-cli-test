language: node_js
node_js:
  - lts/*
before_install:
  - sudo apt-get update
  - sudo apt-get install -y libsecret-1-dev

notifications:
  slack:
    if: branch = main
    on_pull_requests: false
    on_success: never
    on_failure: change
    rooms: $SLACK_TOKEN
